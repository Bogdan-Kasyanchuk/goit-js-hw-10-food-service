const refs = {
  themeSwitchEl: document.querySelector('#theme-switch-toggle'),
  bodyEl: document.querySelector('body'),
  menuEl: document.querySelector('.js-menu'),
};

export const { themeSwitchEl, bodyEl, menuEl } = refs;
